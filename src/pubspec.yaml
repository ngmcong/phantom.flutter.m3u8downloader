name: m3u8downloader
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0-164.0.dev

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.4
  http: ^1.2.2
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
